import { DetailCard, Formulario } from "../cards"
import { NavBar } from "../header"

export const DetailPage = () => {
    return (
        <>
        <NavBar/>
        <DetailCard/>
        <Formulario />
        </>
    )
}